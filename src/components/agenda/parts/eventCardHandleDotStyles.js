import { StyleSheet } from "react-native";

export const eventCardHandleDot = {
  width: 14,
  height: 14,
  borderRadius: 7,
  backgroundColor: "#fff",
  borderWidth: 2,
  borderColor: "#607180",
  position: "absolute",
  zIndex: 100,
  elevation: 10,
};

export default StyleSheet.create({
  eventCardHandleDot,
});
