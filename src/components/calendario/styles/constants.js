import { Dimensions } from "react-native";
import { colors, spacing } from "../../../theme";

export const { width: SCREEN_W } = Dimensions.get("window");

export const MONTH_CELL_H = 46;
export const DAY_CIRCLE = 34;
export const DAY_FONT = 15;

export const H_PADDING = spacing.sm;
export const LINE_COLOR = colors.divider;
